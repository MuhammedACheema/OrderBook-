#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <iomanip>

// classes are basically stuff that create an object for ex a car class, would consist of wheels, color, doors, year etc, thats the same thing we're doing w the order book.

class OrderBook{
    public:
        int id;
        int order_quantity;
        double order_price; 

    
};